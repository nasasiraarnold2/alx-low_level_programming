#include <stdio.h>
#include "main.h"

/**
*  * main - _islower checks if the character is lowercase
*   * @c: a charcter argument
*    * Return: returns 1 and 0 depending on condition
*/
int main(int c);

{
	return (c >= 'a' && c <= 'z');
}
